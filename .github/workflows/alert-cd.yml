name: Alert CD Staging

on:
  push:
    branches:
      - "develop/alert-microservice"
    paths:
      - "alert-microservice/**"
      - "testing-microservice/**"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  deploy-staging:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up SAM
        uses: aws-actions/setup-sam@v2

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: SAM Build
        run: sam build
        working-directory: alert-microservice

      - name: SAM Deploy Staging
        run: sam deploy --no-confirm-changeset --no-fail-on-empty-changeset --config-env staging
        working-directory: alert-microservice

      - name: Install testing dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r testing-microservice/requirements.txt

      - name: Create reports directory
        run: mkdir -p testing-microservice/reports

      - name: Run alert staging tests
        run: |
          pytest testing-microservice/tests/alert \
            --env staging \
            --junitxml=testing-microservice/reports/alert-staging-results.xml

      - name: Generate alert staging report
        run: |
          python testing-microservice/generate_report.py \
            --xml testing-microservice/reports/alert-staging-results.xml \
            --service alert \
            --env staging \
            --out testing-microservice/reports/alert-staging-report.txt

      - name: Upload alert staging test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: alert-staging-test-report
          path: |
            testing-microservice/reports/alert-staging-results.xml
            testing-microservice/reports/alert-staging-report.txt
