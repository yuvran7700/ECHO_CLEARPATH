name: Weather CI

on:
  pull_request:
    branches:
      - "main"
      - "develop/weather-microservice"
    paths:
      - "weather-microservice/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r weather-microservice/requirements.txt
          pip install -r requirements-dev.txt

      # 1. LINTING + FORMATTING CHECK
      - name: Run Ruff
        run: |
          ruff format --check weather-microservice/
          ruff check weather-microservice/

      # 2. UNIT TESTING
      - name: Run unit tests
        run: pytest weather-microservice/tests/unit/

      # 3. INTEGRATION TESTING
      - name: Run integration tests
        run: pytest weather-microservice/tests/integration/

      # 4. COVERAGE TESTING
      - name: Run coverage
        run: |
          coverage run -m pytest weather-microservice/tests/
          coverage report
          coverage xml -o weather-microservice/coverage.xml
          coverage html -d weather-microservice/htmlcov

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: weather-testing-report
          path: |
            weather-microservice/htmlcov/
            weather-microservice/coverage.xml
